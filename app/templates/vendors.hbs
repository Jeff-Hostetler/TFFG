<div class="siteBody siteTop">
  <div class="header">
    <div class="new-link">

        {{#link-to 'vendors.new'}}
          New Vendor
        {{/link-to}}

    </div>
    <h3>Vendors</h3>
  </div>
  <hr>

  {{#each vendor in model}}
    <div class="tableRow">
      {{#link-to 'vendor' vendor}}
        {{vendor.name}}
      {{/link-to}}
    </div>

  {{/each}}
</div>


{{outlet}}
